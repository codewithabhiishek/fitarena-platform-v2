"use client";

// src/app/page.js
// Root page — renders the full FitArena single-page app.
// All routing is handled internally by FitArena component state.

import FitArena from "../components/FitArena";

export default function Page() {
  return <FitArena />;
}
